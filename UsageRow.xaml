<UserControl x:Class="ClaudeUsageOverlay.UsageRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="False"
             IsTabStop="False">

    <!-- One metric, as a column. Columns sit side by side, so the divider and the padding
         that separates them belong to the column on the right. -->
    <Border x:Name="Divider"
            BorderBrush="{StaticResource TrackBrush}"
            BorderThickness="0">

        <Grid Width="188">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="LabelText"
                           Grid.Column="0"
                           Style="{StaticResource LabelStyle}"
                           VerticalAlignment="Bottom"
                           Margin="0,0,8,2" />

                <TextBlock x:Name="PercentText"
                           Grid.Column="1"
                           Style="{StaticResource PercentStyle}"
                           MinWidth="50" />
            </Grid>

            <ItemsControl x:Name="Cells"
                          Grid.Row="1"
                          Height="7"
                          Margin="0,6,0,0"
                          Focusable="False">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1" Columns="24" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

            <TextBlock x:Name="ResetText"
                       Grid.Row="2"
                       Style="{StaticResource MetaStyle}"
                       Margin="0,6,0,0" />
        </Grid>
    </Border>
</UserControl>
